/**
 * 生图工具定义
 * 定义 AI 可调用的 AI 图片生成相关工具
 * 支持通义万相（阿里云）、DALL-E（OpenAI）、文心一格（百度）三种服务商
 */

export const imageTools = [
    {
        type: "function",
        function: {
            name: "generate_image",
            description: `根据文字描述画出一张图片，并直接展示给用户。

【什么时候用】
- 用户希望你画一张图、生成一张图、画个XX给他看
- 用户想把某个画面、形象、场景变成图片
- 用户希望你能"看"到他描述的东西

【什么时候不用】
- 用户只是闲聊或问问题
- 用户想发表情包（用表情包工具）
- 用户想看已有的图（用发送图片工具）

【参数】
- prompt：想画的内容，描述越具体效果越好。可以包含：画面主体、艺术风格、构图视角、光线氛围等。例如："一只橘色的猫趴在窗台上，午后的阳光斜照进来，写实风格，柔和的光线"
- size：图片尺寸，不填用默认正方形
- style：画风，仅在部分模型下有效

【使用要点】
- 调用前可以用角色的口吻告诉用户你在画，但不要说"调用工具"、"启动模块"、"绘图模块"之类的话，保持自然
- 画好后图片会自动展示给用户，不需要再说"图片已发送"
- 用你自己的角色风格告诉用户画好了，比如简单说"画好啦"、"给你~"等，可以简单介绍画面内容
- 失败时不要重试本工具，用角色口吻告诉用户暂时画不出来，建议稍后再试
- 不要在回复中暴露任何技术细节（如接口名、配置文件、错误代码等），保持沉浸感

【耗时】
画图需要几秒到几十秒不等，请耐心等待结果返回。`,
            parameters: {
                type: "object",
                properties: {
                    prompt: {
                        type: "string",
                        description: "图片描述提示词。建议详细描述：主体对象、艺术风格、构图视角、光线氛围等。例如：'一只橘色的猫坐在窗台上，阳光透过玻璃洒下，写实风格，柔和光线'"
                    },
                    size: {
                        type: "string",
                        description: "图片尺寸。通义万相支持 1024*1024、720*1280、1280*720；DALL-E 支持 1024x1024、1792x1024、1024x1792；文心一格支持 1024*1024、1024*1536、1536*1024。不填则使用默认尺寸"
                    },
                    style: {
                        type: "string",
                        description: "图片风格，仅通义万相 wanx-v1 模型有效。可选：<auto>、<photography>、<portrait>、<3d cartoon>、<anime>、<oil painting>、<watercolor>、<sketch>、<chinese painting>、<flat illustration>"
                    }
                },
                required: ["prompt"]
            }
        }
    }
];

export default imageTools;

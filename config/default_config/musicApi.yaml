# 自建音乐API服务设置

# 是否启用自建API服务（随Yunzai启动/停止）
enabled: true

# 服务监听端口（端口冲突时自动+1，最多3次）
port: 3210

# 监听地址（仅本机访问，外部不可访问）
host: 127.0.0.1

# 请求超时时间（毫秒）
timeout: 15000

# 缓存配置
cache:
  # 缓存过期时间（秒）
  ttl: 1800
